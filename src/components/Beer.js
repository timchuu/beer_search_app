import React from 'react';
import BeerContainer from '../containers/beer_container'
const Beer = () => {
    return (
        <div>
            <BeerContainer/>
        </div>
    );
};

export default Beer;